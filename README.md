# dlm - Download Manager

Server and CLI client written in Deno to facilitate downloading videos files in a queue.

Run the server:

```
deno run -A server.ts serve
```

Run the cli (communicates over http):

```
deno run -A cli.ts count
```